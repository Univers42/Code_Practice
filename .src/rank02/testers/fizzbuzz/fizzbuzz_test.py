from typing import Any

# Each case: a list of argv (strings passed after the program name),
# or {"argv": [...], "stdin": "..."}.
# fizzbuzz takes no arguments; a single run is enough.
TEST_CASES: list[Any] = [
    [],
]
